# Запрос времени в секундах у пользователя
total_seconds = int(input("Введите время в секундах: "))

# Вычисление часов, минут и оставшихся секунд
hours = total_seconds // 3600
minutes = (total_seconds % 3600) // 60
seconds = total_seconds % 60

# Форматирование строки для вывода времени в формате чч:мм:сс
time_format = "{:02d}:{:02d}:{:02d}".format(hours, minutes, seconds)

# Вывод времени на экран
print("Время: ", time_format)
